# Custom pretix plugins
# This file lists custom plugins to be installed during Docker build
# Plugins can be specified as:
# - Local paths (relative to repository root): -e ./local/pretix-cliques
# - Git repositories: git+https://github.com/user/pretix-plugin.git
# - PyPI packages: pretix-plugin-name

# Install pretix-cliques from local directory (development)
-e ./local/pretix-cliques

# Install pretix-ticket-transfer from local directory (development)
-e ./local/pretix-ticket-transfer

# Example: Install from Git repository (commented out for local development)
# git+https://github.com/pretix-unofficial/pretix-cliques.git@master

# Install pretix-ticketbourse from Git repository
#git+https://git.kulturkosmos.de/ticketing/pretix-ticketbourse.git

# Install pretix-fontpack-free from PyPI
#pretix-fontpack-free

# Example: Install from PyPI (if published)
# pretix-cliques

